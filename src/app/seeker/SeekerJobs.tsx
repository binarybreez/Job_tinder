import { StyleSheet, Text, View } from "react-native";
import React from "react";
import { SafeAreaView } from "react-native-safe-area-context";

const SeekerJobs = () => {
  return (
    <SafeAreaView>
      <View>
        <Text>SeekerJobs</Text>
      </View>
    </SafeAreaView>
  );
};

export default SeekerJobs;

const styles = StyleSheet.create({});
